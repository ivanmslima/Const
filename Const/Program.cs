using System;
using System.Globalization;

namespace curso
{
    class Progam
    {
        static void Main(string[] args)
        {
            const bool exemplo = true;

            Console.WriteLine("Exemplo de const");

            /*As constantes são valores imutáveis que são conhecidos no tempo de compilação e 
             * não são alterados durante a vida útil do programa. Constantes são declaradas com o modificador const.
             * Apenas os tipos internos do C# (excluindo System. Object) podem ser declarados como const*//


        }
    }
}
